part of '../screen_lib.dart';

class DriverNavigationMapScreen extends StatelessWidget {
  const DriverNavigationMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
